{
  "name": "sheriyans-mart-backend",
  "version": "1.0.0",
  "description": "JSON Server backend for Sheriyans Mart",
  "scripts": {
    "start": "npx json-server --watch db.json --port 8080"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
